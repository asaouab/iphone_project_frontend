let picture = document.querySelector('.mid');
let container = document.querySelector('.all')
function change(path)
{
    picture.src = path;
}
function color(para)
{
    container.style.background = para;
}
